cmake_minimum_required(VERSION 3.12)
project(modular_exponentiation VERSION 1.0.0)

add_library(
	lib
	./include/modular_exponentiation/power_module.cpp
	./include/modular_exponentiation/power_module.hpp
	)

add_executable(main main.cpp)

target_link_libraries(main PUBLIC lib)

# NTL documentation command
# g++ -g -O2 -std=c++11 -pthread -march=native foo.cpp -o foo -lntl -lgmp -lm
# objective:
# g++ main.cpp -g -O2 -std=c++11 -pthread -march=native -o main -lntl -lgmp -lm && ./main
